---
import { getLangFromUrl, useTranslations } from 'src/i18n/utils';
import { getEntry } from 'astro:content';

const general = await getEntry('settings','general');

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<header class="block relative">
  <div class="flex flex-col">
    <div class="flex flex-col w-full mx-auto px-8 py-16 md:max-w-7xl">
      <div class="flex grow flex-wrap">
        <a href="#WhereToFind" class="flex w-auto basis-0 grow min-w-0 max-w-full items-center flex-[0_0_auto] gap-6 hover:underline">
          <!-- <span class="no-underline relative block pointer-events-auto overflow-hidden w-12">
            <Image 
              src={general.data.logo} 
              class="align-middle relative block z-10 w-full h-auto max-w-full border-0" 
              width="48" 
              alt={general.data.title}>
          </span> -->
          <span class="text-2xl font-light -tracking-wider">
            <span class="font-sans font-medium">{t('wheretofind.nav')}</span>
              <!-- <span class="font-serif italic">be</span>
              <span class="text-sauvignon-950 font-black">-</span>
              <span class="font-mono">wine</span> -->
              <!-- {general.data.title} -->
              
          </span>
        </a>
      </div>
    </div>
  </div>
</header>
